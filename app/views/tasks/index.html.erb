<h1>Liste des tâches</h1>

<ul>
  <% @tasks.each do |task| %>
    <%= link_to task.title, task_path(task) %><br>
      <%= link_to "delete this task", destroy_task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  <% end %>
</ul>
<%= link_to 'New Task', tasks_new_path %>
